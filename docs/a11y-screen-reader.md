# A11y: Screen reader compatibility layer

WCAG 2.1 compliance for Screen reader compatibility layer in the PayDay platform.

## Implementation
Apply using the `screen-reader` utility in `frontend/src/utils/a11y/`.
