# A11y: Full keyboard navigation support

WCAG 2.1 compliance for Full keyboard navigation support in the PayDay platform.

## Implementation
Apply using the `keyboard-navigation` utility in `frontend/src/utils/a11y/`.
